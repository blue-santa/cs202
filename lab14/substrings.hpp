/**
 * substrings.hpp
 * CS 201
 * Bryan Beus
 * October 4, 2019
 * Header file for substrings
 */

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using std::cout;
using std::cin;
using std::endl;
using std::vector;
using std::string;
using std::noskipws;
using std::getline;

#ifndef SUBSTRINGSHPP
#define SUBSTRINGSHPP

void exercise1(string & input);
void requestInput(string & input);
void printShrinking(string & input);
void printShrinkingMore(string & input);
void exercise2(string & input);

#endif
